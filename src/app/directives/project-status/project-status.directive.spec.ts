import { ProjectStatusDirective } from './project-status.directive';

describe('ProjectStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectStatusDirective();
    expect(directive).toBeTruthy();
  });
});
